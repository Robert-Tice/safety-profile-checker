project Safety_Base is

   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Main use ("main.adb");

   package Check is
      for Default_Switches ("ada") use ("-rules", "-from=rules/safety_base.txt");
   end Check;

end Safety_Base;

